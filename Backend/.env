PORT=4000
FRONTEND_URL=http://localhost:5173
PYTHON_SERVICE_URL=http://localhost:5000
# Interval between streamed rows in ms (rows are emitted at a random delay
# between STREAM_INTERVAL_MIN and STREAM_INTERVAL_MAX)
STREAM_INTERVAL_MIN=800
STREAM_INTERVAL_MAX=1500
